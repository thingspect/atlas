version: "3"
services:
  postgres:
    image: postgres:11-alpine
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=atlas_test
  mqtt:
    image: eclipse-mosquitto:1.6
    ports:
      - "127.0.0.1:1883:1883"
